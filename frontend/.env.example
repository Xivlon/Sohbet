# Frontend Environment Variables
# Copy this file to .env.local and update with your actual values

# Backend API URL (use https:// for production, http://localhost:8080 for local development)
NEXT_PUBLIC_API_URL=https://your-backend-domain.com

# WebSocket URL (use wss:// for production with :8081 port, ws://localhost:8081 for local development)
# For production on Fly.io: wss://your-backend-domain.com:8081
NEXT_PUBLIC_WS_URL=wss://your-backend-domain.com:8081

# Note: These variables must start with NEXT_PUBLIC_ to be accessible in the browser
# Never commit .env.local to version control
