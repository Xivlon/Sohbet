# Production Environment Variables for Sohbet Frontend
# These values are used when deploying to Vercel

# Backend API URL (Fly.io deployment)
NEXT_PUBLIC_API_URL=https://sohbet-uezxqq.fly.dev

# WebSocket URL (Fly.io deployment)
NEXT_PUBLIC_WS_URL=wss://sohbet-uezxqq.fly.dev:8081

# Enable WebSocket connections
NEXT_PUBLIC_ENABLE_WEBSOCKET=true

# Note: These variables must start with NEXT_PUBLIC_ to be accessible in the browser
